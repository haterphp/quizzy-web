import { IPalette } from "../interfaces";

import { DEFAULT_BUTTON_PALETTE } from "./button";
import { DEFAULT_ICON_PALETTE } from './icon';

export const DEFAULT_PALETTE: IPalette = {
    button: DEFAULT_BUTTON_PALETTE,
    icon: DEFAULT_ICON_PALETTE
}
